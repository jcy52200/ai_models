# ============================================
# 素居家具电商后端 - 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# ============================================

# --------------------------------------------
# AI 配置
# --------------------------------------------
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# --------------------------------------------
# CORS 跨域配置 - 允许的域名列表
# 多个域名使用逗号分隔，不要加空格
# --------------------------------------------

# 本地开发环境
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 生产环境示例 (部署到 Render 时使用)
# 将你的 Vercel 域名填入此处
# CORS_ORIGINS=https://your-project.vercel.app

# 多个域名示例
# CORS_ORIGINS=https://your-project.vercel.app,https://www.yourdomain.com

# --------------------------------------------
# 数据库配置 (可选，默认使用 SQLite)
# --------------------------------------------
# DATABASE_URL=sqlite:///./suju.db

# --------------------------------------------
# JWT 密钥 (可选，默认自动生成)
# 生产环境建议设置固定值，避免重启后 token 失效
# --------------------------------------------
# SECRET_KEY=your-secret-key-here-min-32-chars-long

# --------------------------------------------
# 调试模式 (可选，默认 True)
# 生产环境请设置为 False
# --------------------------------------------
# DEBUG=False
