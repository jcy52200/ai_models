# ============================================
# 素居家具电商前端 - 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# ============================================

# --------------------------------------------
# API 基础 URL 配置
# Vite 要求环境变量必须以 VITE_ 开头才能在客户端使用
# --------------------------------------------

# 本地开发环境
VITE_API_BASE_URL=http://localhost:8000/v1

# 生产环境示例 (部署到 Vercel 时使用)
# 将你的 Render 后端 URL 填入此处
# VITE_API_BASE_URL=https://your-backend.onrender.com/v1

# --------------------------------------------
# 访问密钥配置（可选）
# 用于限制网站访问，部署到 Vercel 时设置
# --------------------------------------------

# 本地开发 - 不设置则跳过验证
VITE_ACCESS_KEY=

# 生产环境示例 - 设置后访问网站需输入密钥
# VITE_ACCESS_KEY=your-secret-access-key

# --------------------------------------------
# 注意事项:
# 1. 修改此文件后需要重启开发服务器
# 2. 以 VITE_ 开头的变量才能在代码中通过 import.meta.env 访问
# 3. .env 文件不应提交到 Git，已添加到 .gitignore
# --------------------------------------------
